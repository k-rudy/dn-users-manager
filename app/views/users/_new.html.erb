<%= form_for @user, url: create_users_path, method: :get do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.label :status %>
  <%= f.text_field :status %>
  
  <%= f.label :role %>
  <%= f.collection_select :role, Role.all, :id, :name %>
  
  <%= f.submit %>
<% end %>
